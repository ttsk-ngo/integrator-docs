# Overview

Integrator 2.0 system is built on two applications: API app and frontend app.

## API
The API application is built to allow easy exchange of frontends.

## Frontend
Frontend application can be either SPA or server-side. It must have the ability to act as OIDC client.